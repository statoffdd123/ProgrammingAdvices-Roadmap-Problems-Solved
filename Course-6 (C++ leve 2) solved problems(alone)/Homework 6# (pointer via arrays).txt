#include <iostream> 
#include <vector>
using namespace std;

int main()
{
	int a = 0;
	int arr[5] { 55,32,15,8,2 };	
	int* pitr = arr;

	int size = sizeof(arr) / sizeof(arr[0]);

	//the adresses are those
       int i = 0;
	for (int i = 0; i <= size; i++)
	{
		printf("the adress of the element %d : %d \n", i + 1, pitr + i);
	}

	//the values are those

	for (int i = 0; i <= size; i++)
	{
		printf("the value of the element %d : %d \n", i + 1, pitr[i]);
	}
}