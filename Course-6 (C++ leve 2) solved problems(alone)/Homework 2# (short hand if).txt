#include<iostream>

void Check_number_PN0(int Number)
{
    std::string Result = "";
	Result = (Number >= 0) ? (Number == 0) ? "number is Zero" : "number is positive" : "Number is negative";
	std::cout << Result;
}
int main()
{
	int number = 0;
	std::cin >> number;
	Check_number_PN0(number);

	return 0;
}